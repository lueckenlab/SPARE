pseudobulk:
  pca_euclidean:
    layer: "X_pca"
    distance: "euclidean"
  scvi_sample_id_cosine:
    layer: "X_scANVI_scRNASeq_sample_ID"
    distance: "cosine"
grouped_pseudobulk:
  pca_euclidean:
    layer: "X_pca"
    distance: "euclidean"
  scvi_sample_id_cosine:
    layer: "X_scANVI_scRNASeq_sample_ID"
    distance: "cosine"
random_vector:
  dimensionality_30:
    latent_dim: 30
    seed: 42
  dimensionality_10:
    latent_dim: 10
    seed: 42
mrvi:
  mrvi:
    max_epochs: 100
scpoli:
  scpoli_sample_embeddings:
    max_epochs: 50
