viash_version: 0.9.1

name: sample_representation_benchmark
organization: luecken_lab
version: dev

source: src
target: target

# config_mods: |
#   .functionality.version := '0.2.3'
#   .platforms[.type == 'nextflow'].directives.tag := '$id'
